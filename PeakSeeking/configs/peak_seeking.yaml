env:
  size: 20
  max_steps: 100
  threshold_ratio: 0.98
  peak_value_min: 5.0
  peak_value_max: 10.0

training:
  episodes: 50000
  learning_rate: 0.2
  discount_factor: 0.95
  epsilon_start: 1.0
  epsilon_end: 0.05
  epsilon_decay: 0.99
  seed: 42
  eval_interval: 20
  log_interval: 10
  render_mode: null

evaluation:
  episodes: 5
  render_mode: "human"
